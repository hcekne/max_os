Templates for creating new notes. Do not store instance content here — templates only.

- Filename: `TPL - Subject.md`.
- Each template should be a complete, working skeleton — copy and fill, no required external steps.
- Include frontmatter with `type:` set to the note kind.
- When a template changes structurally, surface the implication in the next workspace hygiene review rather than back-filling all existing notes immediately.
- Cross-reference `00_System/Indexes.md` and the matching folder's `.instructions.md` when conventions differ from siblings.
