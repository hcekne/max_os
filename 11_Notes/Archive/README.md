# Archive - Notes

This folder stores notes that are no longer active but kept for context/history.

## Archiving rule
- Active canonical note should live in `11_Notes/` root.
- Superseded drafts/syntheses move here and should have `status: archived`.

## Usage
- Keep this folder for reference context only.
- Avoid linking archived notes in active strategy indexes unless there is a specific historical reason.
