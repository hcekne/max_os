# Max OS — Claude Code

Max OS is a plain-Markdown operating system for work.

The canonical agent entry-point lives in [AGENTS.md](AGENTS.md) — read it for the bootstrap order, core rules, local setup check, and validation hook. This file exists so Claude Code finds something at `CLAUDE.md`; the substance lives in `AGENTS.md` and `SYSTEM/LLM Operating Manual.md`.
