---
type: system-state
last_interaction_date:
active_week_plan:
active_quarter_plan:
active_two_year_plan:
active_goals: []
active_modules: []
last_consolidated:
---

# State

What is currently true in this workspace. Small, factual, current.

- Update `last_interaction_date` when a session does real work.
- `active_week_plan`, `active_quarter_plan`, and `active_two_year_plan` link
  the current horizon notes in `PLAN/` — leave them empty until a plan
  exists; set them after a planning review.
- `active_goals` lists links to current goal notes in `PLAN/Goals/`.
- `active_modules` lists intentionally enabled `AUTOMATE/Modules/` packs.
- `last_consolidated` is written by the nightly consolidation routine when
  that automation is enabled; leave it alone otherwise.
- Facts only: history belongs in `SYSTEM/Log.md`, lessons in
  `SYSTEM/Memory.md`.
